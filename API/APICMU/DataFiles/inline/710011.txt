Label('Local Variables Demo #1', 200, 20, size=20)
Label('Click anywhere (gold on left, navy on right)', 200, 50, size=20)

def onMousePress(mouseX, mouseY):
    if (mouseX < 200):
        color = 'gold'
    else:
        color = 'navy'
    Circle(mouseX, mouseY, 30, fill=color)