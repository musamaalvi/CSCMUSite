Label('Random Values Demo #1', 200, 20, size=20, bold=True)
Label('Press keys to change the radius', 200, 40, size=16)

c = Circle(200, 200, 50, fill=gradient('red', 'blue'))

def onKeyPress(key):
    c.radius = randrange(20, 150)
