# (1) Click anywhere on the canvas. The ball moves up and the platform rotates.
# (2) Now click as fast as you can for 10 seconds. See how it looks like a ball
#     bouncing on the platform as it rotates?
# That's it, let's code!

app.background = gradient('midnightBlue', 'dodgerBlue', start='top')

# Define the platform and ball variables
### (HINT: You can't see the left-top of the platform so try to position it by
#          using a different alignment!)
# platform =
# ball =

def onMousePress(mouseX, mouseY):
    # Rotate the platform and bounce the ball.
    ### (HINT: Change the ball's bottom so that it matches the platforms top)
    pass

##### Place your code above this line, code below is for testing purposes #####
# test case:
onMousePress(200, 200)
