# Edit the gradients of these 4 rectangles so the drawing matches the
# solution.  Note that the gradients only use the colors
# red, orange, yellow, hotPink, or blue.
Rect( 40, 50, 70, 70, fill=gradient('hotPink', 'yellow', 'blue', start='left'))
Rect(125, 50, 70, 70, fill=gradient('red', 'yellow', 'hotPink', 'yellow', 'red', start='right-top'))
Rect(210, 50, 70, 70, fill=gradient('hotPink', 'blue', start='left-top'))
Rect(295, 50, 70, 70, fill=gradient('red', 'orange', 'yellow', 'blue'))